include "blackduck-artifactory"
include "blackduck-artifactory-common"

rootProject.name = 'blackduck-artifactory'
rootProject.children.each { project ->
    project.buildFileName = "${project.name}.gradle"
}